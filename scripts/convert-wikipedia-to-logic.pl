#!/usr/bin/perl -w

# this is a really stupid idea, but I thought I would try it.  The
# idea is to iterate over sentences in Wikipedia, and just try to
# parse them with Attempto Parsing Engine.  The idea is that a very
# few of them are probably correctly formed, and will parse.
# Eventually, maybe can use machine translation to cajole them, but
# the point is this should do for now, just to populate *some* data.


